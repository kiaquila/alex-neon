{
  "name": "alex-neon",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-repository.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "preflight": "npm run check && npm test && npm --prefix website run check"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
